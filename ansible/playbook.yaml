- name: Install Java using custom Ansible role
  hosts: all
  become: true
  vars:
    java_distribution: "adoptium"
    java_version: "8"
    install_path: "/opt/java"
  roles:
    - role: java_install
      tags: ["java"]
